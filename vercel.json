{
	"installCommand": "corepack pnpm install",
	"buildCommand": "corepack pnpm build && corepack pnpm --filter typed-i18n-react-demo build",
	"outputDirectory": "apps/react-demo/dist",
	"framework": "vite",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/index.html"
		}
	]
}
