{
	"buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm build && cd apps/react-demo && pnpm build",
	"outputDirectory": "apps/react-demo/dist",
	"installCommand": "pnpm install --frozen-lockfile",
	"framework": "vite",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/index.html"
		}
	]
}
